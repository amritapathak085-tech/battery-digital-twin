/**
 * PLACEHOLDER - Member C builds the real screen here.
 * Sliders for charge cap / temperature / DoD, calling
 * POST /vehicles/{id}/simulate on debounced change.
 */
export default function SimulationPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-100 mb-2">Simulation</h1>
      <p className="text-sm text-gray-500 mb-6">
        Member C&apos;s screen goes here - what-if sliders and before/after comparison.
      </p>
      <div className="bg-card border border-gray-800 rounded-xl p-6 text-sm text-gray-600">
        Coming soon
      </div>
    </div>
  );
}
